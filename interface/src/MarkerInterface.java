
public interface MarkerInterface {
	
	/* An interface does not contain any abstract methods is called as marker Interface 
	 * by implementing marker interface we get some feature through JVM
	 * our own marker interface is possible but for that customized JVM is required*/
	
	
}
